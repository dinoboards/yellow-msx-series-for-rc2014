name: yellow-msx

on:
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'

jobs:
  yellow-msx:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: pull docker tool chain
        run: docker pull vipoo/yellow-msx-rc2014-tool-chain:0.0.19

      - name: clean
        run: docker run --rm --privileged=true -v ./:/src/ -t vipoo/yellow-msx-rc2014-tool-chain:0.0.19 make clean

      - name: release-notes
        run: docker run --rm --privileged=true -v ./:/src/ -t vipoo/yellow-msx-rc2014-tool-chain:0.0.19 make release-notes

      - name: build-jeds
        run: docker run --rm --privileged=true -v ./:/src/ -t vipoo/yellow-msx-rc2014-tool-chain:0.0.19 make release-build-jeds

      - name: build-msx-apps
        run: docker run --rm --privileged=true -v ./:/src/ -t vipoo/yellow-msx-rc2014-tool-chain:0.0.19 make release-build-msx-apps

      - name: build-msx-roms
        run: docker run --rm --privileged=true -v ./:/src/ -t vipoo/yellow-msx-rc2014-tool-chain:0.0.19 make release-build-msx-roms

      - name: build-rc2014-apps
        run: docker run --rm --privileged=true -v ./:/src/ -t vipoo/yellow-msx-rc2014-tool-chain:0.0.19 make release-build-rc2014-apps

      - name: build-release-candidate
        run: docker run --rm --privileged=true -v ./:/src/ -t vipoo/yellow-msx-rc2014-tool-chain:0.0.19 make release-build-package

      - name: Archive release candiate elements
        uses: actions/upload-artifact@v3
        with:
          name: release-candidate
          path: |
            release
            msx/bin
